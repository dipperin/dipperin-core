package config

var (
	Normal_TRANSACTION_GAS = int64(2100)
	DEFAULT_GAS_PRICE      = int64(1)
	// BloomBitsBlocks is the number of blocks a single bloom bit section vector
	// contains on the server side.
	BloomBitsBlocks uint64 = 4096
)
